// ltl-mlp-tiny/main.ltl
//
// Tiny multi-layer perceptron with backprop demo.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-mlp-tiny v0.1.0")
    io::println("Tiny multi-layer perceptron with backprop demo.")
    0
}
